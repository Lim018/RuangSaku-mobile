class AppConstants {
  AppConstants._();

  // Border Radius
  static const double radiusXS = 8.0;
  static const double radiusSM = 12.0;
  static const double radiusMD = 16.0;
  static const double radiusLG = 22.0;
  static const double radiusXL = 28.0;
  static const double radiusXXL = 32.0;
  static const double radiusCard = 40.0;
  static const double radiusHeader = 45.0;
  static const double radiusHero = 50.0;
  static const double radiusIcon = 44.0;

  // Padding
  static const double paddingSM = 8.0;
  static const double paddingMD = 16.0;
  static const double paddingLG = 24.0;
  static const double paddingXL = 32.0;
  static const double paddingScreen = 24.0;

  // Sizes
  static const double navBarHeight = 96.0;
  static const double heroImageHeight = 320.0;
  static const double roomCardWidth = 260.0;
  static const double splashIconSize = 144.0;

  // Durations
  static const Duration screenTransition = Duration(milliseconds: 500);
  static const Duration staggerDelay = Duration(milliseconds: 80);
  static const Duration pressScale = Duration(milliseconds: 150);
  static const Duration toastDuration = Duration(milliseconds: 2200);
}
